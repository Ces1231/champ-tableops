# Simulation

Place challenge-provided MuJoCo/world/robot assets here after the official Intel track starter stack is confirmed.

Do not commit large model checkpoints or datasets directly to Git unless the challenge explicitly requires it.
